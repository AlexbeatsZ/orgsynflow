# OrgSynFlow workspaces

This directory stores local workspace JSON files created by the React workbench.

User-created `*.json` files are ignored by Git by default. The committed
`example-workspace.json` file documents the shape used by the application.
